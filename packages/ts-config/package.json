{
  "name": "@coolcolduk/ts-config",
  "version": "5.1.0",
  "description": "Typescript config with packages",
  "license": "MIT",
  "main": "tsconfig.json",
  "files": [
    "tsconfig.json",
    "README.md"
  ],
  "scripts": {
    "clean:dist": "rm -rf dist && rm -rf tsconfig.tsbuildinfo",
    "clean:node": "rm -rf node_module",
    "lint": "exit 0",
    "pack": "npm pack --pack-destination=../"
  },
  "dependencies": {
    "@types/node": "^24",
    "ts-node": "^10.9.2",
    "typescript": "^6"
  }
}
